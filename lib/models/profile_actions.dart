enum TrackingState { signIn, register, signOut, editProfile, trackingOff, trackingWatching, trackingOn }

enum ProfileAction { signIn, register, signOut, editProfile, trackingOff, trackingWatching, trackingOn }

enum UserSignInStatus { signedOut, processing, signedIn, error }

enum AuthScreenType { signIn, register }

enum Gender {
  male,
  female,
  unspecified,
}
